%%% <siunitx>
%%% ---------------------------------------------------------------------------
\sisetup{
%	exponent-product = \cdot,	% Produktzeichen für Exponentialnotierung
%	free-standing-units,		% Einheitenmakros auch außerhalb der 
%						%  SI-Umgebungen verwenden
%	use-xspace,				% Leerzeichen nach freistehenden Einheiten
	per-mode = symbol-or-fraction,% Darstellung des \per-Makros
%	sticky-per = true,		% \per gilt für alle folgenden Einheiten
%	list-final-separator = { und },	% Abschluss für Einheiten-Liste
%	range-phrase = { bis },		% Verbindungszeichen für Einheiten-Bereich
%	parse-numbers = false,		% Nummern nicht parsen (schneller)
%	parse-units = false,		% Einheiten nicht parsen (schneller)
	retain-unity-mantissa = false	% Anzeige 10e5 statt 1x10e5
}